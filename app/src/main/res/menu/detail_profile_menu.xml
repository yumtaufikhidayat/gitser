<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/nav_favorite_profile"
        android:title="@string/tvFavorite"
        android:icon="@drawable/ic_favorite_unchecked"
        app:showAsAction="ifRoom"/>

    <item
        android:title="@string/tvOtherMenus"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_more">

        <menu>

            <item
                android:id="@+id/nav_share_detail_profile"
                android:title="@string/tvShare"
                android:icon="@drawable/ic_share_red"
                app:showAsAction="never"/>

            <item
                android:id="@+id/nav_open_browser_detail_profile"
                android:title="@string/tvOpenInBrowser"
                android:icon="@drawable/ic_language"
                app:showAsAction="never"/>

        </menu>

    </item>

</menu>